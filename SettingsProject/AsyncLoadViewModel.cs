#nullable enable

namespace SettingsProject
{
    internal sealed class AsyncLoadViewModel
    {
        public string Text { get; }

        public AsyncLoadViewModel(string text)
        {
            Text = text;
        }
    }
}